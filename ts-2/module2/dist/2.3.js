"use strict";
{
    // const rollNumbers : number[] = [3,6,8];
    const rollNumbers = [3, 6, 8];
    // const mentors: string[] = ['Mr.x', 'Mr.y', 'My.z'];
    const mentors = ['Mr.x', 'Mr.y', 'My.z'];
    // const boolArray:boolean[] = [true, false,true];
    const boolArray = [true, false, true];
    const use = [
        { name: 'Saurav', age: 100 },
        { name: 'Sarkar', age: 200 },
    ];
    const add = (x, y) => x + y;
    add(30, 20);
    const manuser = ['Mr.x', 'Mr.y'];
    const uderId = [1234, { name: 'saurav', email: 'saurav@gmail.com' }];
}
